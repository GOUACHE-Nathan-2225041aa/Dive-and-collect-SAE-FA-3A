<div class="max-w-2xl mx-auto p-8">
    <div class="bg-white rounded-lg shadow-lg overflow-hidden">
        <div class="bg-lagon p-6 text-white">
            <h1 class="text-2xl font-bold text-center">Nouveau message de contact</h1>
        </div>
        <div class="p-6">
            <p class="text-gray-700 mb-4">Un nouveau message a été envoyé via le formulaire de contact :</p>
            <div class="bg-gray-50 rounded-lg p-4 mb-4">
                <p class="text-gray-600"><strong>Nom :</strong> {{ name }}</p>
                <p class="text-gray-600"><strong>Email :</strong> {{ userEmail }}</p>
            </div>
            <div class="bg-gray-50 rounded-lg p-4">
                <p class="text-gray-600"><strong>Message :</strong></p>
                <p class="text-gray-700 mt-2">{{ message }}</p>
            </div>
        </div>
    </div>
</div>
