<div id="confirm-removal" class="fixed inset-0 flex items-center justify-center bg-black bg-opacity-50 hidden">
    <div class="bg-white p-8 rounded-lg min-w-[30%]">
        <div class="w-full flex justify-end">
            <button id="cr-close-btn" class="p-2">
                <svg class="w-6 h-6 text-gray-500 hover:text-gray-800" fill="none" stroke="currentColor"
                     viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                          d="M6 18L18 6M6 6l12 12"></path>
                </svg>
            </button>
        </div>
        <div>
            <p class="text-outremer">Confirmez la suppression, attention toutes les images liées à cette plongée seront également
                supprimées.</p>
            <div class="flex justify-center gap-4 mt-4">
                <a href="{{ path('app_dive_remove', {diveId : dive.id}) }}"
                   class="bg-fushia text-white font-bold rounded-xl p-3 items-center justify-center inline-block">
                    Confirmer
                </a>
                <button id="cancel-removal" class="bg-fushia text-white font-bold rounded-xl p-3 items-center justify-center inline-block">
                    Retour
                </button>
            </div>
        </div>
    </div>
</div>
